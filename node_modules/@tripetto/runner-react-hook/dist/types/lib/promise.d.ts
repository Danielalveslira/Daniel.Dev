import { MutableRefObject } from "react";
export interface IPromise {
    readonly resolve: () => void;
    readonly reject: () => void;
    handle: number;
}
export declare const promise: (ref: MutableRefObject<IPromise | undefined>) => {
    resolve: () => void;
    reject: () => void;
    cancel: () => void;
};
