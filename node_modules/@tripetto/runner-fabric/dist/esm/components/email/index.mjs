import { InputFabric } from "../input/index.mjs";

const EmailFabric = (props) =>
    InputFabric(
        Object.assign(Object.assign({ type: "email", inputMode: "email", autoComplete: "email" }, props), {
            placeholder: props.placeholder || "@",
        })
    );

export { EmailFabric };
