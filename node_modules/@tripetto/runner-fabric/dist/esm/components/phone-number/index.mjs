import { InputFabric } from "../input/index.mjs";

const PhoneNumberFabric = (props) =>
    InputFabric(
        Object.assign(Object.assign({ type: "tel", inputMode: "tel", autoComplete: "tel" }, props), {
            placeholder: props.placeholder || "+1 (201) 555-5555",
        })
    );

export { PhoneNumberFabric };
